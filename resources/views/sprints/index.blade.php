<x-app-layout>
    <x-slot name="header"><div class="flex items-center justify-between"><h2 class="text-xl font-semibold text-slate-900">Sprints</h2><a href="{{ route('sprints.create') }}" class="rounded-md bg-indigo-600 px-4 py-2 text-sm font-semibold text-white">Nova sprint</a></div></x-slot>
    <div class="mx-auto max-w-7xl space-y-4 px-4 py-8 sm:px-6 lg:px-8">
        <div class="overflow-x-auto rounded-lg bg-white shadow-sm"><table class="min-w-full divide-y divide-slate-200 text-sm"><thead class="bg-slate-50"><tr><th class="px-4 py-3 text-left">Nome</th><th class="px-4 py-3 text-left">Período</th><th class="px-4 py-3 text-left">Status</th><th class="px-4 py-3 text-right">Ações</th></tr></thead><tbody class="divide-y divide-slate-100">@forelse ($sprints as $sprint)<tr><td class="px-4 py-3 font-medium">{{ $sprint->nome }}</td><td class="px-4 py-3">{{ $sprint->data_inicio->format('d/m/Y') }} a {{ $sprint->data_fim->format('d/m/Y') }}</td><td class="px-4 py-3 capitalize">{{ str_replace('_', ' ', $sprint->status) }}</td><td class="space-x-3 px-4 py-3 text-right"><a class="text-indigo-600" href="{{ route('sprints.show', $sprint) }}">Ver</a><a class="text-amber-600" href="{{ route('sprints.edit', $sprint) }}">Editar</a>@can('delete', $sprint)<form class="inline" method="POST" action="{{ route('sprints.destroy', $sprint) }}">@csrf @method('DELETE')<button class="text-red-600" onclick="return confirm('Remover sprint?')">Excluir</button></form>@endcan</td></tr>@empty<tr><td colspan="4" class="px-4 py-8 text-center text-slate-500">Nenhuma sprint cadastrada.</td></tr>@endforelse</tbody></table></div>
    </div>
</x-app-layout>
