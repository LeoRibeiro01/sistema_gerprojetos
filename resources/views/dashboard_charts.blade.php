<section class="mt-6 rounded-xl border border-slate-200 bg-white p-5 shadow-sm" x-data="{ showBuilder: false }">
    <div class="flex flex-wrap items-center justify-between gap-3"><div><p class="text-xs font-semibold uppercase tracking-wide text-indigo-600">Visão personalizada</p><h2 class="mt-1 text-lg font-semibold text-slate-900">Meus gráficos</h2></div><button type="button" @click="showBuilder = !showBuilder" class="rounded-md bg-slate-800 px-3 py-2 text-sm font-semibold text-white">Criar gráfico</button></div>
    <form x-show="showBuilder" x-cloak method="POST" action="{{ route('dashboard.charts.store') }}" class="mt-4 grid gap-3 rounded-lg bg-slate-50 p-4 md:grid-cols-4">@csrf<input name="titulo" required placeholder="Título do gráfico" class="rounded-md border-slate-300 text-sm"><select name="metrica" class="rounded-md border-slate-300 text-sm"><option value="task_status">Status das tarefas</option><option value="overdue">Atrasos</option><option value="billable_hours">Horas faturáveis</option><option value="productivity">Produtividade</option></select><select name="tipo" class="rounded-md border-slate-300 text-sm"><option value="bar">Colunas</option><option value="pie">Pizza</option><option value="line">Linha</option></select><label class="flex items-center gap-2 rounded-md border border-slate-300 bg-white px-3 text-sm">Cor<input name="cor" type="color" value="#4f46e5" class="h-7 w-10 border-0 p-0"></label><button class="rounded-md bg-indigo-600 px-3 py-2 text-sm font-semibold text-white md:col-span-4">Adicionar ao dashboard</button></form>
    <div class="mt-5 grid gap-5 lg:grid-cols-2">@forelse($charts as $chartItem) @php($chart = $chartItem['model']) @php($data = $chartItem['data']) @php($max = max($data['values'] ?: [1]))<article class="rounded-lg border border-slate-200 p-4"><div class="flex items-start justify-between gap-3"><div><h3 class="font-semibold text-slate-900">{{ $chart->titulo }}</h3><p class="text-xs text-slate-500">{{ ucfirst($chart->tipo) }}</p></div><div class="flex gap-3 text-xs"><a href="{{ route('dashboard.charts.pdf', $chart) }}" target="_blank" class="font-semibold text-indigo-600">PDF</a><form method="POST" action="{{ route('dashboard.charts.destroy', $chart) }}">@csrf @method('DELETE')<button class="font-semibold text-red-600">Excluir</button></form></div></div>@if($chart->tipo === 'pie')<div class="mx-auto mt-5 h-40 w-40 rounded-full" style="background: conic-gradient({{ $chart->cor }} 0deg {{ ($data['values'][0] / max(1, array_sum($data['values']))) * 360 }}deg, #cbd5e1 {{ ($data['values'][0] / max(1, array_sum($data['values']))) * 360 }}deg 360deg)"></div>@else<div class="mt-5 flex h-40 items-end gap-2">@foreach($data['values'] as $index => $value)<div class="group flex h-full flex-1 flex-col justify-end"><div class="rounded-t" style="height: {{ $max ? ($value / $max) * 100 : 0 }}%; background-color: {{ $chart->cor }}" title="{{ $data['labels'][$index] }}: {{ $value }}"></div><span class="mt-2 truncate text-center text-[10px] text-slate-500">{{ $data['labels'][$index] }}</span></div>@endforeach</div>@endif</article>@empty<p class="text-sm text-slate-500">Crie um gráfico para acompanhar produtividade, horas ou status.</p>@endforelse</div>
</section>
