<x-app-layout><x-slot name="header"><h2 class="text-xl font-semibold text-slate-900">{{ $sprint->nome }}</h2></x-slot><div class="mx-auto max-w-3xl space-y-5 px-4 py-8"><div class="rounded-lg bg-white p-6 shadow-sm"><p><strong>Período:</strong> {{ $sprint->data_inicio->format('d/m/Y') }} a {{ $sprint->data_fim->format('d/m/Y') }}</p><p class="mt-2"><strong>Status:</strong> {{ ucfirst($sprint->status) }}</p><p class="mt-2 whitespace-pre-line"><strong>Meta:</strong> {{ $sprint->meta ?: 'Não definida.' }}</p></div><div class="rounded-lg bg-white p-6 shadow-sm"><h3 class="font-semibold">Tarefas ({{ $sprint->tarefas->count() }})</h3><ul class="mt-3 divide-y divide-slate-100 text-sm">@forelse($sprint->tarefas as $tarefa)<li class="py-2">{{ $tarefa->titulo }} <span class="text-slate-500">({{ str_replace('_', ' ', $tarefa->status) }})</span></li>@empty<li class="py-2 text-slate-500">Nenhuma tarefa nesta sprint.</li>@endforelse</ul></div></div></x-app-layout>
