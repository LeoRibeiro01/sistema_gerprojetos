<x-app-layout><x-slot name="header"><h2 class="text-xl font-semibold text-slate-900">{{ $epic->titulo }}</h2></x-slot><div class="mx-auto max-w-3xl space-y-5 px-4 py-8"><div class="rounded-lg bg-white p-6 shadow-sm"><p><strong>Projeto:</strong> {{ $epic->projeto->titulo }}</p><p class="mt-2"><strong>Status:</strong> {{ str_replace('_', ' ', $epic->status) }}</p><p class="mt-4 whitespace-pre-line">{{ $epic->descricao ?: 'Sem descrição.' }}</p></div><div class="rounded-lg bg-white p-6 shadow-sm"><h3 class="font-semibold">User stories</h3><ul class="mt-3 divide-y divide-slate-100 text-sm">@forelse($epic->userStories as $story)<li class="py-2">{{ $story->titulo }} <span class="text-slate-500">({{ $story->pontos ?? 0 }} pontos)</span></li>@empty<li class="py-2 text-slate-500">Nenhuma user story vinculada.</li>@endforelse</ul></div></div></x-app-layout>
